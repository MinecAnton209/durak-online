# A long, random string used to sign the session ID cookie.
# For production, use a strong, randomly generated string.
SESSION_SECRET=

# The connection string for your PostgreSQL database.
# This is used when DB_CLIENT is set to 'postgres'.
# Leave blank if you are using SQLite for local development.
DATABASE_URL=

# Defines which database client to use.
# Possible values: 'sqlite' (for local development) or 'postgres' (for production).
DB_CLIENT=sqlite