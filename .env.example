# Application Port
PORT=3000

# Session secret key
SESSION_SECRET=your_super_secret_key

# VAPID keys for web push notifications
# Generate them using `npx vapid --applicationServerKey --json`
VAPID_SUBJECT=mailto:your-email@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Database configuration
# Use 'sqlite' for local development or 'postgres' for production
DB_CLIENT=sqlite
# For postgres, provide the connection string, e.g., postgresql://user:password@host:port/database
DATABASE_URL=

# Cookie domain for production environment
# Example: .yourdomain.com
COOKIE_DOMAIN=

# CORS origin for the admin panel
ADMIN_CORS_ORIGIN=http://localhost:5173

# Node environment
# Set to 'production' in production
NODE_ENV=development